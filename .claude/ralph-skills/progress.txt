# Ralph Progress Log

## Codebase Patterns
- FinancePulse 使用 LangGraph 进行工作流编排
- 股票代码格式：A股如 "600519"，创业板如 "300750"
- 三种模式：tradingagents(8阶段), aihedgefund(4层), fusion(Leader决策)
- 参考仓库位于 ~/FinancePulse_reference/TradingAgents/ 和 ai-hedge-fund/
- 前端组件位于 src/components/analysis/
- 后端agents位于 server/agents/tradingagents/

---

## 2026-04-26 09:52 - US-001
- Created news.py analyst in agents/tradingagents/researchers/
- Created modular run_news_analyst(state, llm_service) function
- Integrated into tradingagents_workflow.py Stage 3

**Learnings:**
- Pattern: Each analyst module has run_*_analyst(state, llm_service)
- Pattern: Output format is JSON with _report (string) and _signal (dict)
---

## 2026-04-26 (earlier run) - US-008
- Created AnalysisParams.tsx component
- Supports mode selection (TradingAgents/AIHedgeFund/Fusion)
- Supports leader selection from 19 masters
- Supports convergence parameters (max_iterations, convergence_gap, early_stop_gap)
- Supports risk level (conservative/moderate/aggressive)
---

## 2026-04-26 (earlier run) - US-009
- Created SignalPanel.tsx component
- Displays 5 analyst signals with confidence bars
- Shows bull vs bear debate results
- Shows convergence status

---

## 2026-04-26 (earlier run) - US-010
- Created RecommendationCard.tsx component
- Displays action: buy/hold/sell/watch
- Shows confidence, timeframe, position_size, stop_loss, exit_price
- Shows risk warnings list
- Adapts format based on mode

---
## 2026-04-26 10:30 - US-003
- Created risk_debate.py with three risk perspectives (conservative/moderate/aggressive)
- Each perspective has distinct system prompts and risk parameters
- Integrated into workflow.py as Stage 7 node (between debate and synthesize)
- Outputs risk_debate_results, risk_debate_history, and risk_recommendation

**Learnings:**
- Pattern: Debates use _build_*_prompt(state) and _parse_*_response(content) pattern
- Pattern: Three risk levels map to stop_loss_pct: conservative (5-8%), moderate (10-15%), aggressive (15-25%)
- Gotcha: Workflow routing goes debate -> (conditional) -> risk_debate -> synthesize -> decision
- Reference: ai-hedge-fund/src/agents/risk_manager.py has volatility-based position sizing
---


## 2026-04-26 10:35 - US-004
- Created agents/leaders/base.py with LeaderBase abstract class
- Implemented 6 investment masters: Warren Buffett, Ben Graham, Peter Lynch, Charlie Munger, Michael Burry, Nassim Taleb
- Implemented create_leader(leader_id) factory function
- Implemented run_leader(leader_id, state, llm_service) async function
- Each leader has _build_prompt(state) and _parse_response(content) methods
- Each leader has detailed system prompts with investment philosophy

**Learnings:**
- Pattern: Leaders use config dict with system_prompt embedded
- Pattern: Each leader implements _build_prompt(state) and _parse_response(content)
- Pattern: Output format is JSON with decision, confidence, reasoning, key_factors, risk_factors
- Pattern: Leaders import AgentState from graph.state and LLMService from services.llm
- Gotcha: Python 3.9 uses `from typing import Optional` instead of `type | None` syntax

## 2026-04-26 10:45 - US-005
- Created server/agents/leaders/warren_buffett.py with WarrenBuffettLeader class
- Implemented 7-dimension analysis framework:
  - Business quality (moat analysis)
  - Profitability (ROE, ROA, margins)
  - Growth (revenue/profit growth)
  - Financial health (debt, cash flow)
  - Intrinsic value (DCF, DDM)
  - Margin of safety calculation
  - Management quality
- Added create_warren_buffett_leader() factory function
- Added run_warren_buffett_analysis() async convenience function

**Learnings:**
- Warren Buffett analysis uses SEVEN_DIMENSIONS constant list
- Output includes seven_dimensions nested dict with scores for each dimension
- margin_of_safety calculation: (intrinsic_value - current_price) / intrinsic_value
- get_seven_dimensions() helper method returns the 7 dimension labels

## 2026-04-26 11:00 - US-006
- Created server/agents/risk_manager.py with comprehensive risk management
- Implemented volatility calculation functions:
  - calculate_volatility_metrics(prices, lookback_days=60): daily_volatility, annualized_volatility, volatility_percentile
  - calculate_volatility_adjusted_limit(annualized_volatility): position limit % based on volatility
  - calculate_correlation_multiplier(avg_correlation): correlation adjustment
- Implemented run_risk_management(state, llm_service): main risk analysis function
- Implemented analyze_portfolio_risk(state, llm_service): LLM-based risk insights
- Reference: ai-hedge-fund/src/agents/risk_manager.py has similar implementation

**Learnings:**
- Volatility calculation: use np.diff(closes) / closes[:-1] for returns
- Annualized volatility = daily_vol * sqrt(252)
- Position limit mapping: low_vol(<15%) -> 25%, med_vol(15-30%) -> 12.5-20%, high_vol(30-50%) -> 5-15%, very_high(>50%) -> 10%
- Correlation multiplier: high corr (>=0.8) -> 0.7x, neutral (0.4-0.6) -> 1.0x, low corr (<0.2) -> 1.10x


## 2026-04-26 14:00 - US-011
- Updated apiService.ts to support AnalysisMode parameter
- Added AnalyzeStockParams interface with mode, leaderId, convergenceGap, earlyStopGap, riskLevel
- Changed analyzeStock method to accept AnalyzeStockParams object instead of positional parameters
- Typecheck passes

**Learnings for future iterations:**
- Pattern: Use object parameters for API methods to support optional params
- Pattern: AnalysisMode type is defined in apiService.ts and reused in AnalysisParams.tsx
- Gotcha: TypeScript noEmit check passes, no build required for type verification


## 2026-04-26 15:30 - US-012
- Rewrote AnalysisPage.tsx to align with PRD Section 12 layout
- Integrated StockHeader component for stock search and quote display
- Added KlineChart and FundFlowPanel placeholder components
- Integrated AnalysisParams component for parameter configuration
- Integrated SignalPanel to display analyst signals and debate results
- Integrated RecommendationCard to display investment recommendations
- Added SummaryText component to display analysis summary
- Implemented handleStartAnalysis to call apiService.analyzeStock with mode/leaderId/riskLevel params
- Updated SignalPanel and RecommendationCard based on analysis results
- Added analysis state management (idle/analyzing/completed/error)
- Build passes

**Learnings for future iterations:**
- Pattern: Use object parameters for API calls to support optional params
- Pattern: Export AnalysisMode type from AnalysisParams.tsx for use in parent components
- Pattern: AnalysisState type ('idle'|'analyzing'|'completed'|'error') for UI state management
- Gotcha: apiService.getLeaders() requires backend to be running, add fallback in component
- Gotcha: When calling analyzeStock, convert preset strings to numeric params using getConvergencePresets()


## 2026-04-26 16:00 - US-014
- Created comprehensive DataService with all required methods
- Implemented get_stock_data(code): 获取实时行情
- Implemented get_financial_metrics(code, period, limit): 获取财务指标 (ROE, ROA, margins, etc.)
- Implemented search_line_items(code, items, limit): 获取财务明细
- Implemented get_market_cap(code, date): 获取市值
- Implemented get_price_history(code, days): 获取历史价格
- Added AKShare integration with akshare library
- Added mock data fallback when API fails
- Added global singleton get_data_service() function

**Files changed:**
- server/services/data.py (完整重写)

**Learnings for future iterations:**
- Pattern: Use `USE_MOCK_DATA` env var to enable mock data for testing
- Pattern: Each method has `_fetch_*_akshare` for real data and `_get_mock_*` for fallback
- Gotcha: AKShare functions like `stock_zh_a_hist` use code without sh/sz prefix
- Gotcha: Use `_safe_float()` helper to handle None/NaN values from pandas
- Pattern: Mock data for specific stocks (600519, 000858, 300750) provides realistic base values
- Reference: ai-hedge-fund/src/tools/api.py uses similar pattern with cache + API fallback
